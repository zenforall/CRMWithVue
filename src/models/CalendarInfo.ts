//import type { VIEWS } from "./CalendarInfoConstants";

interface CalendarInfo {
  today:Date,
  next:boolean,
  prev:boolean,
  actualDay:number,
  actualMonth:number,
  actualYear:number,
  choosedDate:Date
}
